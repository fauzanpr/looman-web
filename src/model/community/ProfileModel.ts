export interface CommunityProfileModel {
    data: {
        id: number,
        user_community_name: string,
        community_name: string,
        email: string,
        phone_number: string,
        full_address: string,
        city: string
    }
}